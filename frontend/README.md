# Frontend (React + Vite + Tailwind)

Install and run:

```bash
cd frontend
npm install
npm run dev
```

The dev server runs at `http://localhost:5173` by default. The frontend expects the backend API at `http://localhost:5000/api` (set `VITE_API_URL` to change).
